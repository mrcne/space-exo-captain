"use client"

import ObjectsTable from "@/components/components/ObjectsTable";

export default function ExploreContainer() {
  return (
    <div>
      <ObjectsTable onSelect={() => {}} />
    </div>
  );
}
